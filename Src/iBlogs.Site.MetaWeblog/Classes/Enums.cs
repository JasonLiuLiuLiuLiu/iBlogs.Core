namespace iBlogs.Site.MetaWeblog.Classes
{
    public enum PostStatus
    {
        Draft,
        Pending,
        Private,
        Publish
    }

    public enum CommentStatus
    {
        Hold,
        Approve,
        Spam
    }
}